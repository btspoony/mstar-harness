# Moved

`{KNOWLEDGE_DIR}`, `{ITERATION_DIR}`, and `{SPECS_DIR}` division now live in the **`mstar-plan-artifacts`** skill:

- Read **`mstar-plan-artifacts`** `SKILL.md`
- Details: **`mstar-plan-artifacts/references/knowledge-and-designs.md`**

Do not duplicate rules here; update the topic skill only.
