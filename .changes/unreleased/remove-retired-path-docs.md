---
packages: root, opencode
---

- **Docs describe current state only**: removed retired-path prose from runtime skills — `{PLAN_DIR}/reports/` (legacy report location), `designs/` compat-read fallback for `{SPECS_DIR}`, legacy flat delivery-compass naming, and the retired `{HARNESS_DIR}/notes.json` (no code creates it; runtime notes live in `workflows/<id>/notes.jsonl`). v1→v2 migration guards and supported `.agents/` discovery are unchanged.

<!-- CN -->
- **文档只描述现状**：从运行时 skills 中移除已废弃路径的描述 —— `{PLAN_DIR}/reports/`（旧报告目录）、`designs/` 对 `{SPECS_DIR}` 的兼容读回退、legacy 扁平 delivery-compass 命名、以及已停产的 `{HARNESS_DIR}/notes.json`（无代码创建；运行时 notes 走 `workflows/<id>/notes.jsonl`）。v1→v2 迁移护栏与受支持的 `.agents/` 发现链保持不变。
