---
category: Changed
packages: engine
---

- Added the canonical execution-session transport: independently acquired caller identities, mint-once local launch identities, strict wire references, revision-preserving resume, and synchronous current-session assertions.
- Preserved suspended historical handoff submitters and held-lease provenance without restoring authorization; appended nullable migration coverage metadata.

<!-- CN -->
- 新增规范执行会话传输：独立获取的调用者身份、一次性本地启动身份、严格 wire 引用、保持修订号的恢复，以及同步当前会话断言。
- 保留挂起历史交接提交者与持有租约来源，但不恢复授权；追加可为空的迁移覆盖元数据列。
