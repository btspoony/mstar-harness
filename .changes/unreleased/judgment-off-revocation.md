---
category: Harness
packages: root
---

- **Judgment runtime:** observing `jev_mode=off` during input collection now aborts the run, prevents further pilot/pack reads, and ignores late read results.

<!-- CN -->
- **Judgment runtime：**在输入收集期间观察到 `jev_mode=off` 时会中止运行、阻止后续 pilot/pack 读取，并忽略迟到的读取结果。
