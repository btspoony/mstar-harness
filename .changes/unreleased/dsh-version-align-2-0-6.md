---
packages: root
---
- **dsh plugin**: version aligned to `2.0.6` (single-version invariant — the `@mstar-harness/engine` devDependency is pinned exact `2.0.6` and the engine version assertions in the dsh suites now expect `2.0.6`), restoring the unified `mstar version` watermark after the upstream v2.0.6 merge.

<!-- CN -->
- **dsh 插件**：版本对齐至 `2.0.6`（单一版本不变量——`@mstar-harness/engine` devDependency 精确钉定 `2.0.6`，dsh 套件中的引擎版本断言更新为 `2.0.6`），在上游 v2.0.6 合入后恢复统一的 `mstar version` 水印。
